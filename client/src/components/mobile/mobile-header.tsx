/**
 * Compact app-style header — re-export SiteHeader with mobile defaults documented here.
 * Implementation lives in site-header.tsx (single source for nav + routing).
 */
export { SiteHeader as MobileHeader } from "@/components/site-header";
