/** Minimum votes before showing raw rating counts publicly. */
export const CREW_RATING_MIN_VOTES_TO_SHOW_COUNT = 10;

/** Hall Approved badge — crew-validated meals. */
export const HALL_APPROVED_MIN_VOTES = 20;
export const HALL_APPROVED_MIN_APPROVAL = 0.8;

/** Top Rated Recipes listing — meaningful sample size. */
export const TOP_RATED_MIN_VOTES = 10;
