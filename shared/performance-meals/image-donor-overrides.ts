/**
 * Performance Meals hero donors — interim copy when AI regen is unavailable.
 * Trust rule: never copy a less accurate image just to be unique.
 * Prefer npm run generate:performance-meals-imagery for slug-specific heroes.
 */
export const PERFORMANCE_MEAL_IMAGE_DONOR_OVERRIDES: Record<string, string> = {
  /** boneless-chicken-thighs-sweet-potato-spinach — requires unique AI hero (see imagery-prompt-overrides). */
};
